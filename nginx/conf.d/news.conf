# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name primeMedia.com www.primeMedia.com admin.primeMedia.com api.primeMedia.com minio.primeMedia.com;
    return 301 https://$host$request_uri;
}

# Main Site (Public)
server {
    listen 443 ssl;
    server_name primeMedia.com www.primeMedia.com;

    ssl_certificate /etc/nginx/certs/nginx.crt;
    ssl_certificate_key /etc/nginx/certs/nginx.key;

    location / {
        proxy_pass http://public:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Admin CMS
server {
    listen 443 ssl;
    server_name admin.primeMedia.com;

    ssl_certificate /etc/nginx/certs/nginx.crt;
    ssl_certificate_key /etc/nginx/certs/nginx.key;

    location / {
        proxy_pass http://admin:3001;
        proxy_set_header Host $host;
    }
}

# API
server {
    listen 443 ssl;
    server_name api.primeMedia.com;

    ssl_certificate /etc/nginx/certs/nginx.crt;
    ssl_certificate_key /etc/nginx/certs/nginx.key;

    location / {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# MinIO
server {
    listen 443 ssl;
    server_name minio.primeMedia.com;

    ssl_certificate /etc/nginx/certs/nginx.crt;
    ssl_certificate_key /etc/nginx/certs/nginx.key;

    location / {
        proxy_pass http://minio:9000;
        proxy_set_header Host $host;
    }
}
